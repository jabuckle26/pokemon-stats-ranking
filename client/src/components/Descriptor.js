import React from "react";

export const Descriptor = () => {
  return (
    <div className="appDescriptor">
      <p>Select a Pokemon type and stat to sort by!</p>
      <p> Click the Nav bar to search again!</p>
    </div>
  );
};
